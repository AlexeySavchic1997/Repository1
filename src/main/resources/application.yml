spring:
  application:
   name: voter_pet_project
  datasource:
   url: jdbc:mysql://localhost:3306/voter_pet_project
   username: Alexey
   password: PetPasword

  jpa:
   hibernate:
    ddl-auto: validate
   show-sql: true
   properties:
    hibernate:
     format_sql: true
     dialect: org.hibernate.dialect.MySQLDialect